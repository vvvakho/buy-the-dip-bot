version: "2"
sql:
  - schema: "database/schema"                # Path to your schema file
    queries: "database/queries"                  # Path to your SQL query files
    engine: "postgresql"                   # Database engine
    gen:
      go:
        out: "internal/db"                 # Output directory for generated files
